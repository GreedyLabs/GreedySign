# GreedySign — TODO

> 앞으로 진행해야 할 작업 목록입니다. 완료된 기능은 README 와 사용자 가이드를 참고하세요.
> 우선순위: **P1** 높음 / **P2** 중간 / **P3** 낮음

---

## 대량 배포 개선

| # | 기능 | 설명 | 우선순위 |
|---|------|------|---------|
| D-8 | 리마인더 / 자동 만료 | `expires_at` 초과 문서 자동 expired, 주간 미응답 리마인더 이메일 | **P2** |
| D-10 | 캠페인 복제 (Duplicate) | 기존 캠페인을 템플릿 유지한 채 복제해 새 수신자 집합으로 재사용 | **P3** |
| D-11 | 캠페인 일시중지 (Pause) | 발송 큐를 정지해 수신자에게 추가 메일이 가지 않도록 일시 차단 | **P3** |

## 프론트엔드

| # | 기능 | 설명 | 우선순위 |
|---|------|------|---------|
| F-3 | 서명 기한 D-Day 배지 | 문서 목록 / 에디터에 `expires_at` 기반 배지 | **P2** |
| F-4 | 문서 필터 / 정렬 | 상태별 필터, 날짜·이름 정렬 | **P3** |
| F-5 | 대시보드 기한 패널 | "곧 기한이 다가오는 문서" 사이드 패널 | **P3** |
| F-6 | 모바일 반응형 | 사이드바 → 하단 탭바, 에디터 터치 조작 | **P3** |
| F-7 | 수신자 행 배치 작업 | 체크박스 선택 → "선택한 N명 재발송 / 제외" 일괄 액션 | **P2** |

## 프론트엔드 + 백엔드

| # | 기능 | 설명 | 우선순위 |
|---|------|------|---------|
| B-1 | 순차 서명 (Sequential) | `signing_mode='sequential'` 런타임 로직 (앞사람 완료 후 다음 초대) | **P1** |
| B-2 | 서명 기한 설정 | 문서 / 캠페인 단위 `expires_at` UI + 만료 처리 | **P1** |
| B-7 | 초대 만료 자동 처리 | `expires_at` 경과 초대 자동 상태 변경 (cron 또는 lazy) | **P2** |
| B-8 | 문서별 감사 로그 API | 소유자용 `GET /documents/:id/audit-logs` | **P2** |
| B-9 | JWT Refresh Token | 현재 7일 고정 → Access (1h) + Refresh (30d) 전환 | **P1** |

## 인프라 · 보안

| # | 기능 | 설명 | 우선순위 |
|---|------|------|---------|
| I-1 | API Rate Limiting | `hono-rate-limiter`: 인증 10/min, 초대 5/min, 팬아웃 발송 rate-limit | **P1** |
| I-2 | AWS S3 마이그레이션 | `storage.ts` 추상화 활용 | **P1** |
| I-3 | Redis Pub/Sub for SSE | 단일 서버 인메모리 SSE → 수평 확장 대응 | **P2** |
| I-5 | Helmet / CORS 강화 | CORS 허용 출처 환경변수화 (기본 보안 헤더는 `hono/secure-headers` 로 적용됨) | **P1** |

---

## 구현 우선순위 추천 (다음 스프린트)

### 즉시 — 안정성
1. **I-1** API Rate Limiting — 특히 캠페인 팬아웃 · 재발송 구간 어뷰징 위험
2. **I-5** Helmet / CORS 강화
3. **B-9** JWT Refresh Token
4. **B-2 / B-7 / D-8** 기한 · 리마인더 시스템 — 현재 `expires_at` 컬럼만 있고 런타임 없음

### 병행 — 확장성
5. **I-2** S3 마이그레이션
6. **I-3** Redis Pub/Sub (SSE) — 멀티 인스턴스 대비

### 이후 — 워크플로우 확장
8. **B-1** 순차 서명 런타임
9. **D-10 / D-11** 캠페인 복제 · 일시중지
10. **F-7** 수신자 일괄 액션
11. **F-3 ~ F-6** 기한 배지 / 필터 / 모바일
